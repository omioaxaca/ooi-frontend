---
title: "Introducción a Grafos"
description: "Entiende qué son los grafos, sus tipos y cómo representarlos en código"
date: "2026-02-09"
author: "OOI Oaxaca"
tags: ["c++", "grafos", "lista de adyacencia", "matriz de adyacencia"]
syllabusId: 54
---

## ¿Qué es un grafo?

**Objetivo:** convertir relaciones del enunciado en estados y conexiones, sin elegir todavía un algoritmo. Pregunta si la relación tiene dirección, peso, lazos, aristas repetidas y si pueden existir vértices aislados.

Un **grafo simple** no tiene lazos ni aristas paralelas. En uno no dirigido, cada arista conecta dos extremos distintos y cuenta una vez, aunque la guardemos dos veces en listas de vecinos. La suma de grados es $2m$; un grafo simple de $n$ vértices tiene como máximo $n(n-1)/2$ aristas.

En un grafo dirigido distinguimos grado de entrada y de salida. Tener un camino de A a B no garantiza uno de B a A. Tampoco confundas un recorrido que puede repetir vértices con un camino simple; muchos problemas especifican cuál permiten.

Un **grafo** es una colección de **nodos** (vértices) conectados por **aristas** (edges). Es la forma más general de representar relaciones entre objetos.

**Analogía:** Piensa en un mapa de ciudades. Las ciudades son los nodos, y las carreteras son las aristas. Algunas carreteras son de un solo sentido (grafo dirigido), otras van en ambas direcciones (no dirigido).

## Tipos de grafos

| Tipo             | Descripción                       | Ejemplo                     |
| ---------------- | --------------------------------- | --------------------------- |
| **No dirigido**  | Las aristas van en ambos sentidos | Amistades en redes sociales |
| **Dirigido**     | Las aristas tienen dirección      | Seguidores en Twitter       |
| **Ponderado**    | Las aristas tienen peso (costo)   | Distancias entre ciudades   |
| **No ponderado** | Todas las aristas pesan igual     | Conexiones de red           |

## Terminología

- **Grado** en un grafo simple no dirigido: cantidad de aristas incidentes. En dirigidos, cuenta entrada y salida por separado.
- **Camino:** Secuencia de nodos conectados por aristas.
- **Ciclo:** Camino que empieza y termina en el mismo nodo.
- **Conexo:** Puedes llegar de cualquier nodo a cualquier otro.
- **Componente conexa:** Grupo máximo de nodos mutuamente alcanzables.
- **Árbol:** Grafo conexo sin ciclos ($n$ nodos, $n-1$ aristas).
- **DAG:** Grafo dirigido acíclico (Directed Acyclic Graph).

## Representación 1: Lista de adyacencia ⭐

La más usada en competencias. Para cada nodo, guardamos la lista de sus vecinos.

```cpp
#include <iostream>
#include <vector>
using namespace std;

const int MAXN = 100005;
vector<int> adj[MAXN];

int main() {
    int n, m;  // n nodos, m aristas
    cin >> n >> m;

    for (int i = 0; i < m; i++) {
        int u, v;
        cin >> u >> v;
        adj[u].push_back(v);
        adj[v].push_back(u);  // Quitar esta línea si es dirigido
    }

    // Imprimir vecinos del nodo 1
    cout << "Vecinos de 1: ";
    for (int v : adj[1]) cout << v << " ";

    return 0;
}
```

**Memoria:** $O(n + m)$ — eficiente.

### Con pesos

```cpp
vector<pair<int,int>> adj[MAXN];  // {vecino, peso}

// Leer
int u, v, w;
cin >> u >> v >> w;
adj[u].push_back({v, w});
adj[v].push_back({u, w});

// Recorrer
for (auto [vecino, peso] : adj[nodo]) {
    // ...
}
```

## Representación 2: Matriz de adyacencia

Una tabla 2D donde `mat[i][j] = 1` si hay arista de `i` a `j`.

```cpp
int mat[1005][1005];  // Solo para n ≤ 1000

int main() {
    int n, m;
    cin >> n >> m;

    for (int i = 0; i < m; i++) {
        int u, v;
        cin >> u >> v;
        mat[u][v] = 1;
        mat[v][u] = 1;  // Si no dirigido
    }

    // ¿Hay arista entre 2 y 5?
    if (mat[2][5]) cout << "Sí" << endl;

    return 0;
}
```

**Memoria:** $O(n^2)$ — solo para grafos pequeños.

## ¿Cuál usar?

| Criterio         | Lista de adyacencia  | Matriz de adyacencia |
| ---------------- | -------------------- | -------------------- |
| Memoria          | $O(n + m)$ ✅        | $O(n^2)$             |
| ¿Hay arista u→v? | $O(\text{grado})$    | $O(1)$ ✅            |
| Recorrer vecinos | $O(\text{grado})$ ✅ | $O(n)$               |
| Grafos grandes   | ✅                   | ❌                   |

La lista suele convenir para grafos dispersos y recorridos. La matriz puede convenir si el tamaño cuadrático cabe y necesitas consultar parejas frecuentemente. Decide según las operaciones y la memoria, no por un umbral universal de vértices.

## Ejemplo: leer y recorrer un grafo

```cpp test
#include <iostream>
#include <vector>
using namespace std;

vector<int> adj[100005];

int main() {
    int n, m;
    cin >> n >> m;

    for (int i = 0; i < m; i++) {
        int u, v;
        cin >> u >> v;
        adj[u].push_back(v);
        adj[v].push_back(u);
    }

    // Imprimir el grafo
    for (int i = 1; i <= n; i++) {
        cout << i << ":";
        for (int v : adj[i]) cout << " " << v;
        cout << endl;
    }

    return 0;
}
```

**Input:**

```text input
5 4
1 2
1 3
2 4
3 5
```

**Output:**

```text output
1: 2 3
2: 1 4
3: 1 5
4: 2
5: 3
```

**Comprueba:** hay cinco vértices y cuatro aristas; los grados 2, 2, 2, 1 y 1 suman 8. Agregar un vértice aislado no agrega ninguna arista, pero sí cambia el número de componentes. Tener $n-1$ aristas no basta para afirmar que un grafo sea árbol: también debe ser conexo.

## Siguiente paso

[Representación de grafos y recorrido](/blog/grafos/representacion-recorridos).
