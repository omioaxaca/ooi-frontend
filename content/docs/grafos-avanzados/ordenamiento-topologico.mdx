---
title: "Ordenamiento Topológico"
description: "Ordena nodos de un DAG respetando las dependencias"
date: "2026-02-09"
author: "OOI Oaxaca"
tags: ["c++", "ordenamiento topológico", "DAG", "Kahn"]
syllabusId: 58
---

## ¿Qué es un ordenamiento topológico?

**Antes de empezar:** distingue orden numérico de orden de dependencias. Puede haber varios órdenes válidos. La condición que debes comprobar es que, para cada arista, el origen aparezca antes que el destino; no que el resultado coincida con un único ejemplo.

**Analogía:** Piensa en las materias de una carrera universitaria. Algunas requieren prerrequisitos: no puedes tomar Cálculo 2 sin Cálculo 1. Un **ordenamiento topológico** es un orden válido para cursarlas todas respetando los prerrequisitos.

Formalmente: dado un **grafo dirigido acíclico (DAG)**, encuentra un orden lineal de los nodos tal que si hay una arista $u \to v$, entonces $u$ aparece antes que $v$.

<Callout type="warning">
  Solo existe en **DAGs**. Si hay un ciclo, no hay ordenamiento topológico
  posible.
</Callout>

## Algoritmo de Kahn (BFS)

Usa los **grados de entrada** (cuántas aristas llegan a cada nodo):

```cpp test
#include <iostream>
#include <vector>
#include <queue>
using namespace std;

const int MAXN = 100005;
vector<int> adj[MAXN];
int gradoEntrada[MAXN];

vector<int> topoSort(int n) {
    queue<int> q;
    vector<int> orden;

    // Agregar nodos con grado de entrada 0
    for (int i = 1; i <= n; i++) {
        if (gradoEntrada[i] == 0) {
            q.push(i);
        }
    }

    while (!q.empty()) {
        int u = q.front(); q.pop();
        orden.push_back(u);

        for (int v : adj[u]) {
            gradoEntrada[v]--;
            if (gradoEntrada[v] == 0) {
                q.push(v);
            }
        }
    }

    // Si no procesamos todos los nodos, hay ciclo
    if (orden.size() != n) {
        return {};  // Ciclo detectado
    }

    return orden;
}

int main() {
    int n, m;
    cin >> n >> m;

    for (int i = 0; i < m; i++) {
        int u, v;
        cin >> u >> v;
        adj[u].push_back(v);
        gradoEntrada[v]++;
    }

    vector<int> orden = topoSort(n);

    if (static_cast<int>(orden.size()) != n) {
        cout << "Hay ciclo, no existe ordenamiento topológico" << endl;
    } else {
        for (int x : orden) cout << x << " ";
        cout << endl;
    }

    return 0;
}
```

Entrada:

```text input
4 3
1 3
2 3
3 4
```

Salida de esta implementación:

```text output
1 2 3 4
```

También `2 1 3 4` sería válido. Kahn elimina un vértice cuyas dependencias pendientes ya son cero; al retirarlo reduce una vez cada arista saliente. Por eso cuesta $O(V+E)$ tiempo y $O(V)$ espacio adicional.

El algoritmo **modifica** los grados de entrada. Si necesitas repetirlo, trabaja con una copia o reconstruye los grados. Si te piden el orden lexicográficamente menor, usa una cola de prioridad de mínimos en lugar de FIFO y considera su coste logarítmico.

**Autocomprobación:** agregar `4 -> 3` crea un ciclo y no se deben presentar los vértices procesados parcialmente como una solución completa. Un vértice aislado también debe aparecer en el orden.

## Algoritmo con DFS

```cpp
vector<int> orden;
bool vis[MAXN];
bool enStack[MAXN];
bool hayCiclo = false;

void dfs(int u) {
    vis[u] = true;
    enStack[u] = true;

    for (int v : adj[u]) {
        if (enStack[v]) { hayCiclo = true; return; }
        if (!vis[v]) dfs(v);
        if (hayCiclo) return;
    }

    enStack[u] = false;
    orden.push_back(u);
}

// En main: hacer DFS desde todos los nodos, luego reverse(orden)
```

Si no se detectó un ciclo y se recorrieron todos los vértices, el ordenamiento topológico es el reverso del orden de finalización de DFS. Si hay ciclo, descarta el orden parcial; invertirlo no lo vuelve válido.

## Ejercicio de práctica

Dadas N tareas y M dependencias (la tarea U debe hacerse antes que V), encuentra un orden válido para completar todas las tareas.

<details>
<summary>Ver solución</summary>

Usa el algoritmo de Kahn mostrado arriba. Si el resultado tiene menos de N elementos, hay dependencias circulares.

</details>
